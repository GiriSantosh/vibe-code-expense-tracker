# PRODUCTION ENVIRONMENT VARIABLES TEMPLATE
# 
# SECURITY WARNING: Replace ALL values with production-grade secrets
# NEVER use these example values in production
#
# Copy this file to .env.production and replace with actual secrets

# Database Configuration
DB_USERNAME=expense_prod_user
DB_PASSWORD=REPLACE_WITH_STRONG_DB_PASSWORD

# Encryption Configuration (CRITICAL - minimum 64 characters for production)
ENCRYPTION_MASTER_KEY=REPLACE_WITH_PRODUCTION_GRADE_ENCRYPTION_MASTER_KEY_MINIMUM_64_CHARACTERS

# Keycloak Admin Configuration
KEYCLOAK_ADMIN_USERNAME=REPLACE_WITH_SECURE_ADMIN_USERNAME
KEYCLOAK_ADMIN_PASSWORD=REPLACE_WITH_STRONG_ADMIN_PASSWORD

# OAuth2 Client Configuration
KEYCLOAK_CLIENT_SECRET=REPLACE_WITH_PRODUCTION_CLIENT_SECRET

# Application URLs (replace with your actual production domains)
REACT_APP_API_BASE_URL=https://api.yourdomain.com
REACT_APP_KEYCLOAK_URL=https://auth.yourdomain.com
REACT_APP_OAUTH2_REDIRECT_URI=https://yourdomain.com/auth/callback
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
KEYCLOAK_AUTH_SERVER_URL=https://auth.yourdomain.com
KEYCLOAK_EXTERNAL_URL=https://auth.yourdomain.com

# Generate strong passwords using:
# openssl rand -base64 32    # For regular passwords
# openssl rand -base64 64    # For encryption keys