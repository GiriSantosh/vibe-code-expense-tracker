# Environment Variables Template for Personal Expense Tracker
# Copy this file to .env and update with your actual values

# Database Configuration
DB_USERNAME=postgres
DB_PASSWORD=password

# Keycloak Configuration
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=admin
KEYCLOAK_CLIENT_SECRET=your-client-secret

# Encryption Configuration (CRITICAL - Generate a strong 32-character key)
ENCRYPTION_MASTER_KEY=your-256-bit-encryption-key-32chars

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8081

# Frontend Configuration
REACT_APP_API_BASE_URL=http://localhost:8080
REACT_APP_KEYCLOAK_URL=http://localhost:8081
REACT_APP_OAUTH2_REDIRECT_URI=http://localhost:3000/auth/callback

# Production URLs (Update for production deployment)
# REACT_APP_API_BASE_URL=https://api.yourdomain.com
# REACT_APP_KEYCLOAK_URL=https://auth.yourdomain.com
# REACT_APP_OAUTH2_REDIRECT_URI=https://yourdomain.com/auth/callback
# CORS_ALLOWED_ORIGINS=https://yourdomain.com

# Security Notes:
# 1. ENCRYPTION_MASTER_KEY must be exactly 32 characters for AES-256
# 2. KEYCLOAK_CLIENT_SECRET should be a strong random string
# 3. Change default passwords in production
# 4. Use HTTPS URLs in production